<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="LOGS" value="${PROP_DIR}/biblioteca/log" />

    <conversionRule conversionWord="highlight" converterClass="org.springframework.boot.logging.logback.ColorConverter" />

    <appender name="ConsoleAppender" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %magenta(%d{yyyy/MM/dd E HH:mm:ss.SSS}) %highlight(%-5p) %blue(%-40.40logger{0}) --- %green(%C{1.}) %red(:%M{1.}) %msg%n%throwable
            </Pattern>
        </layout>
    </appender>

    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/biblioteca.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy/MM/dd E HH:mm:ss.SSS} %-5p --- %C{1.}=>%M{1.} %msg%n%throwable</Pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- rollover daily and when the file reaches 10 MegaBytes -->
            <fileNamePattern>
                ${LOGS}/old/log-%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>1MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>


    <!-- LOG everything at INFO level -->
    <root level="INFO">
        <appender-ref ref="ConsoleAppender" />
    </root>

<!--    <logger name="org.springframework" level="info">-->
<!--        <appender-ref ref="ConsoleAppender" />-->
<!--    </logger>-->
<!--    -->
    <!-- LOG "com.baeldung*" at TRACE level -->
    <logger name="com.darkona.biblioteca" level="trace" additivity="false">
        <appender-ref ref="RollingFile" />
    </logger>

</configuration>